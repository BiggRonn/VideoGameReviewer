<section class="card">
    <a href="/review/{{id}}">
        <header>{{gameCard.title}}</header>
        <div class="card-body">
            <div>Genres: {{gameCard.genre}}</div>
            <div>Platforms: {{gameCard.platform}}</div>
            <div>Description: {{gameCard.description}}</div>
            <div class="form-group">
                <label for="project-desc">Comments:</label>
                <textarea class="form-input" id="project-desc" name="project-desc"></textarea>
            </div>
        </div>
    </a>
</section>

<script src="/js/profile.js"></script>